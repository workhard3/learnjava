server:
  port: 8888
spring:
  datasource:
    username: root
    password: legend
    url: jdbc:mysql://localhost:3306/helloworld?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations:
    classpath:mapper/*Mapper.xml
  type-aliases-package: com.workhard.entity

logging:
  level:
    com:
      example:
        mapper: debug